# Metrics

匿名のUUIDをSHA-256で一方向変換し、イベント名と日付だけをD1へ保存します。勤務先、シフト、時給、金額、メモ、選択日、IPアドレスは保存しません。同じ匿名利用者・イベント・日付は一件にまとめ、35日後に削除します。

| Event             | Meaning                            |
| ----------------- | ---------------------------------- |
| `visited`         | カレンダーまたは関連ページを開いた |
| `workplace_added` | 勤務先を追加した                   |
| `shift_added`     | シフトを追加した                   |
| `month_ready`     | 同じ月へ5件以上のシフトを登録した  |
| `exported`        | CSV、ICS、JSONを書き出した         |
| `returned`        | 別の日に再訪した                   |

中核成果は`month_ready`です。`npm run metrics`は全期間と直近7日の人数、各段階の人数と転換率をJSONで返します。利用者が入力した内容や個別識別子は出力しません。
